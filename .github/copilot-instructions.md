# Copilot instructions

This repository's agent and contributor context — project summary, conventions,
patterns, gotchas, and testing strategy — is maintained in a single
provider-agnostic file: **`AGENTS.md`** at the repository root. Read it and
follow it. Do not duplicate guidance here; edit `AGENTS.md` instead.
