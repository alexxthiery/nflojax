# Agent context

This repository keeps a single, provider-agnostic source of truth for agent and
contributor guidance: **`AGENTS.md`** at the repository root. It is imported
below so Claude Code picks it up automatically. Do not duplicate guidance here —
edit `AGENTS.md` instead.

@AGENTS.md
